#include "main.h"
/**
* _abs - prints absolute value of number
* @r: is the parameter being used.
* Return: absolute value.
*/
int _abs(int r)
{
	if (r < 0)
	{
	return (-1 * r);
	}
	else
	{
	return (r);
	}
}
